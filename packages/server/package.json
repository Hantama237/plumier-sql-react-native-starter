{
  "name": "server",
  "version": "1.0.0",
  "scripts": {
    "start": "ts-node-dev --inspect -- src/index",
    "test": "jest",
    "postbuild": "ts-node scripts/postbuild.ts",
    "build": "tsc -p tsconfig.build.json"
  },
  "dependencies": {
    "@types/dotenv": "^6.1.1",
    "dotenv": "^7.0.0",
    "plumier": "^1.0.0-beta.8"
  },
  "devDependencies": {
    "@types/jest": "^24.0.11",
    "@types/supertest": "^2.0.7",
    "jest": "^24.7.1",
    "supertest": "^4.0.2",
    "ts-jest": "^24.0.2",
    "ts-node-dev": "^1.0.0-pre.32",
    "typescript": "^3.4.5"
  }
}
